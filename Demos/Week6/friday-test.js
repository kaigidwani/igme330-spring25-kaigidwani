const first_name = 'travis';
const last_name = 'stodter';

// Arrow functions, calls have to be made after it is declared

// const make_full_name = (first, last) => {
//     return `${first} ${last}, Maker of Names`;
// }


// This gets pulled to the top so it can be used at any time,
// useful for utility functions at the end of files

// function make_full_name(first, last) {
//     return `${first} ${last}, Maker of Names`;
// }

export {
    first_name,
    make_full_name
}